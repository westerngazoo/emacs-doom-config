# ── Editor ──────────────────────────────────────────────────────────────────
alias e='emacs -nw'
alias emacs='emacs -nw'
alias codefire='emacs -nw'

# ── Prompt ──────────────────────────────────────────────────────────────────
eval "$(starship init zsh)"

# ── ls → eza (Nerd Font icons + git status) ─────────────────────────────────
alias ls='eza --icons --group-directories-first'
alias ll='eza -l --icons --git --group-directories-first'
alias la='eza -la --icons --git --group-directories-first'
alias lt='eza --tree --icons --level=2'

# ── cat → bat (syntax highlighting) ─────────────────────────────────────────
alias cat='bat --style=plain --paging=never'
alias batp='bat --style=full'   # full with line numbers + git markers

# ── CLI bundle aliases ──────────────────────────────────────────────────────
alias lg='lazygit'
alias top='btop'
alias md='glow -p'           # paginated markdown read; e.g. `md README.md`
alias ff='fastfetch'         # system splash

# ── Rust ────────────────────────────────────────────────────────────────────
[ -f "$HOME/.cargo/env" ] && . "$HOME/.cargo/env"

# ── macOS-only ──────────────────────────────────────────────────────────────
if [[ "$OSTYPE" == darwin* ]]; then
  alias ee='open -a Emacs'
  alias lock='pmset displaysleepnow'
  # Auto-start Ollama if not running
  pgrep -x ollama >/dev/null 2>&1 || (ollama serve >/dev/null 2>&1 &)
  zzz() {
    local secs=${1:-0}
    nohup sh -c "sleep $secs && pmset sleepnow" >/dev/null 2>&1 &
    disown
    echo "Sleeping in ${secs}s..."
  }
fi

# Created by `pipx` on 2026-05-17 15:57:30
export PATH="$PATH:/Users/goose/.local/bin"


# Added by Antigravity CLI installer
export PATH="/Users/goose/.local/bin:$PATH"

# Added by Antigravity IDE
export PATH="/Users/goose/.antigravity-ide/antigravity-ide/bin:$PATH"

# ── Smart cd (zoxide must init LAST so its cd hook isn't overridden) ──────────
eval "$(zoxide init zsh --cmd cd)"
